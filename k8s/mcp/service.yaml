apiVersion: v1
kind: Service
metadata:
  name: dakera-mcp
  namespace: dakera
  labels:
    app.kubernetes.io/name: dakera-mcp
    app.kubernetes.io/component: mcp
spec:
  selector:
    app.kubernetes.io/name: dakera-mcp
    app.kubernetes.io/component: mcp
  ports:
    - name: http
      port: 3001
      targetPort: http
      protocol: TCP
  type: ClusterIP
